# 视频

1. ~~左侧菜单除了 全部/收藏,需要可以拖拉移动排序~~ ✅
2. ~~续播没有生效~~ ✅（Media.start + 延迟 seek）
3. ~~自动连播没有生效~~ ✅（宿主播放页内连播 + 上一集/下一集/剧集列表）
4. ~~播放页分享按钮左侧添加收藏~~ ✅
5. ~~合集需要可以拖拉排序~~ ✅
6. ~~二级分类重命名、显示数量、编辑入口~~ ✅


1. ~~左侧菜单,拖拽时,应该显示个悬浮提示拖拽的组件和位置吧~~ ✅
2. ~~自动连播好像是倒序排列的~~ ✅（列表/连播共用 sortSeriesEpisodes）
3. ~~上一集/下一集放到下方播放按钮两侧~~ ✅
4. ~~剧集列表右下角 FAB + 右侧 drawer~~ ✅
5. ~~合集的拖拽排序没有生效~~ ✅（拖拽手柄 + text/plain）
6. ~~二级分类编辑入口看不见~~ ✅（移出 chips 行，固定在工具栏）


1. ~~拖拽虚影与原组件样式一致~~ ✅
2. ~~播放/暂停按钮左右居中~~ ✅
3. ~~剧集列表在控制栏全屏右侧；drawer 打开时右下角同位置关闭~~ ✅
4. ~~合集「自定义」排序 + 拖拽同样式虚影~~ ✅
5. ~~合集右上角菜单：重命名/解散~~ ✅


1. ~~菜单拖拽从上往下落点偏下~~ ✅（向下拖修正 insert 下标）
2. ~~上一集/下一集循环切换~~ ✅
3. ~~drawer 剧集按钮与控制栏同位（贴底右侧）~~ ✅
4. ~~同名合集合并添加~~ ✅（resolveCollectionName + 规范名折叠）
5. ~~合集列表可拖拽排序~~ ✅
6. ~~剧集拖拽松开后清除虚影~~ ✅


1. ~~播放页剧集顺序与合集列表一致；切集后恢复返回按钮~~ ✅
2. ~~添加视频上传进度分层 + 封面/时长限时，避免卡在「上传中」~~ ✅
3. ~~库列表视频+合集均可拖拽；落点高亮；虚影清除；自定义排序~~ ✅


1. ~~连点下一集顶栏偶发消失~~ ✅（顶栏常驻，去掉与 media_kit 冲突的 toggle）
2. ~~拖到组件空隙松开虚影不消失~~ ✅（全窗口可落点 + 就近插入 + dragend/drop 清虚影）
3. ~~全屏后点剧集误关全屏~~ ✅（全屏路由内侧栏 overlay，不用 Scaffold drawer / maybePop）