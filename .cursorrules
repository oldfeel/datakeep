# Cursor Rules for MyData Project

## Project Context
跨平台文件同步应用（基于 Syncthing）。
- 应用：Flutter（桌面 + Android，同一套代码 `mydata_flutter/`）
- 进程内 API：Dart shelf（HTTPS `:8443`）
- 同步引擎：本机 Syncthing（`127.0.0.1:8384`）

详细约定见根目录 `AGENTS.md`。

## Architecture Rules

### 1. Interface Usage
- 桌面与 Android 均通过本机 HTTPS API（`:8443`）访问设备/文件夹
- Syncthing REST 仅由后端代理，前端不直连
- 设备 `deviceID == "local"` 或本机真实 ID 表示本机特殊处理

### 2. Code Style
- 中文注释和用户界面文本
- 使用 TypeScript/Dart 类型安全模式（本项目以 Dart 为主）
- 错误通过 SnackBar / 明确错误态提示用户

### 3. State Management
- Flutter：Provider / 页面内 State + 加载与错误态
- 操作后刷新列表（尤其删除）
- 保持 UI 与 Syncthing/后端状态同步

### 4. File Organization
- Flutter UI：`mydata_flutter/lib/`
- Dart 后端：`mydata_flutter/lib/core/backend/`
- 旧 Go 后端（停维）：`mydata_flutter/backend/`

### 5. Development Workflow
- `cd mydata_flutter && flutter run -d linux` / `-d android`
- Android 原生库：`mydata_flutter/start_android.sh`
- 后端改动随 Flutter 热重载/热重启生效（进程内 shelf）

## Key Features
- 文件夹管理（添加、编辑、删除、共享权限）
- 设备管理与共享
- 文件浏览与同步
- 跨平台桌面 + Android

## Important Notes
- 过滤设备时注意本机（`local` / 本机 deviceID）
- 破坏性操作需要确认对话框
- 使用明确的加载与错误提示
- UI 模式可参考 `syncthing/gui/`
